# SWIM Platform — Environment Variables
# Copy this file to .env and fill in real values.

# ---------- Required ----------

# Google Gemini API key (used by all agents)
GEMINI_API_KEY=your-gemini-api-key-here
GOOGLE_API_KEY=your-google-api-key-here

# ---------- Optional: API Security ----------

# Comma-separated API keys for the REST gateway (leave empty to disable auth)
SWIM_API_KEYS=

# Secret for signing/verifying JWT tokens (leave empty to disable JWT auth)
SWIM_JWT_SECRET=

# ---------- Optional: Overrides ----------

# Override the LLM model (default: models/gemini-2.5-flash)
# SWIM_LLM_MODEL=models/gemini-2.5-flash

# Log level (default: INFO)
# SWIM_LOG_LEVEL=DEBUG

# PREDIKT model type: ensemble | lstm | sarima | rule_based
# PREDIKT_MODEL_TYPE=ensemble

# ---------- Optional: Alerting ----------

# SMTP settings for email alerts
# SWIM_SMTP_HOST=smtp.gmail.com
# SWIM_SMTP_PORT=587
# SWIM_SMTP_USER=
# SWIM_SMTP_PASSWORD=
# SWIM_ALERT_RECIPIENTS=admin@example.com

# Slack webhook for alerts
# SWIM_SLACK_WEBHOOK_URL=
